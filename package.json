{
  "name": "lowkey-cooked-root",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client",
    "vercel-build": "npm install --prefix client && npm run build --prefix client"
  }
}
